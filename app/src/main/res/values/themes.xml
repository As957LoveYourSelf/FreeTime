<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FreeTime" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_blue</item>
        <item name="colorPrimaryVariant">@color/theme_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/theme_blue</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="LoadingPage" parent="Theme.FreeTime">
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="WordDistinguish" parent="Theme.FreeTime">
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="SuperResolution" >
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="ClassReservation" >
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="TableDetail">
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="ChangeInfo">
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="ChangePassword">
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="InfoDetail">
        <item name="android:windowBackground">@drawable/loading_page</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/theme_blue</item>
    </style>


</resources>